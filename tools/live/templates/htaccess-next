# Every filename under _next/static carries a content hash, so a byte that
# changes changes the URL. These can be cached until the heat death of the
# universe and a deploy is still picked up instantly.
#
# GENERATED — edit tools/live/templates/htaccess-next.

<IfModule mod_headers.c>
    Header set Cache-Control "public, max-age=31536000, immutable"
</IfModule>

Options -Indexes
